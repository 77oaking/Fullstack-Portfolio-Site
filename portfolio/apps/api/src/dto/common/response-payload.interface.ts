/**
 * Re-export of the shared response envelope so API code can use the canonical
 * type without depending on the libs path alias inside DTO files.
 */
export type { ResponsePayload } from '@portfolio/shared-types';
